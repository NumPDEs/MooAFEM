% handleChangedMesh Handle consequences to FeSpace if underlying mesh has
%   changed.
%
% See also: FeSpace, listener

function handleChangedMesh(obj, src, event)

obj.dofs = [];
obj.freeDofs = [];

end
